'use client'

import { useEffect, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

type ClassRow={id:string;name:string}; type Subject={id:string;name:string}; type Assignment={id:string;class_id:string;subject_id:string;title:string;description:string|null;due_at:string|null;created_at:string}
export default function AssignmentsPage(){
 const supabase=createClient(); const [classes,setClasses]=useState<ClassRow[]>([]); const [subjects,setSubjects]=useState<Subject[]>([]); const [rows,setRows]=useState<Assignment[]>([]); const [classId,setClassId]=useState(''); const [subjectId,setSubjectId]=useState(''); const [title,setTitle]=useState(''); const [description,setDescription]=useState(''); const [dueAt,setDueAt]=useState(''); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [error,setError]=useState(''); const [message,setMessage]=useState('')
 async function load(){setLoading(true);setError('');const [c,s,a]=await Promise.all([supabase.from('classes').select('id,name').order('name'),supabase.from('subjects').select('id,name').order('name'),supabase.from('assignments').select('id,class_id,subject_id,title,description,due_at,created_at').order('created_at',{ascending:false}).limit(50)]);if(c.error||s.error||a.error)setError((c.error||s.error||a.error)?.message||'Could not load assignments.');setClasses((c.data||[]) as ClassRow[]);setSubjects((s.data||[]) as Subject[]);setRows((a.data||[]) as Assignment[]);setLoading(false)}
 useEffect(()=>{load()},[])
 async function create(){if(!classId||!subjectId||!title.trim()){setError('Class, subject and title are required.');return}setSaving(true);setError('');setMessage('');const {error:e}=await supabase.rpc('create_assignment',{p_class_id:classId,p_subject_id:subjectId,p_title:title.trim(),p_description:description.trim()||null,p_due_at:dueAt?new Date(dueAt).toISOString():null,p_attachment_url:null});if(e)setError(e.message);else{setMessage('Assignment published.');setTitle('');setDescription('');setDueAt('');await load()}setSaving(false)}
 const className=(id:string)=>classes.find(c=>c.id===id)?.name||'Class'; const subjectName=(id:string)=>subjects.find(s=>s.id===id)?.name||'Subject'
 return <main className="min-h-screen bg-slate-50 p-6 md:p-10"><div className="mx-auto max-w-7xl"><a href="/dashboard" className="text-sm font-semibold text-blue-600">← Dashboard</a><h1 className="mt-2 text-3xl font-bold text-slate-900">Assignments</h1><p className="mt-1 text-slate-500">Publish work, exams and learning documents for your assigned classes and subjects.</p>{error&&<p className="mt-6 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}{message&&<p className="mt-6 rounded-xl bg-green-50 px-4 py-3 text-sm text-green-700">{message}</p>}<section className="mt-8 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold">New assignment</h2><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-sm font-medium">Class<select value={classId} onChange={e=>setClassId(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-3"><option value="">Select class</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="text-sm font-medium">Subject<select value={subjectId} onChange={e=>setSubjectId(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-3"><option value="">Select subject</option>{subjects.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label className="text-sm font-medium">Title<input value={title} onChange={e=>setTitle(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-3" placeholder="Homework / Exam / Project" /></label><label className="text-sm font-medium">Due date & time<input type="datetime-local" value={dueAt} onChange={e=>setDueAt(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-3" /></label><label className="text-sm font-medium md:col-span-2">Description<textarea value={description} onChange={e=>setDescription(e.target.value)} rows={4} className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-3" /></label></div><button onClick={create} disabled={saving} className="mt-4 rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white disabled:opacity-50">{saving?'Publishing...':'Publish assignment'}</button></section><section className="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 p-5"><h2 className="font-semibold">Recent assignments</h2></div>{loading?<p className="p-5 text-slate-500">Loading...</p>:<div className="divide-y divide-slate-100">{rows.map(r=><div key={r.id} className="p-5"><div className="flex flex-col gap-2 md:flex-row md:items-center md:justify-between"><div><p className="font-semibold text-slate-900">{r.title}</p><p className="text-sm text-slate-500">{className(r.class_id)} · {subjectName(r.subject_id)}</p></div><p className="text-sm text-slate-500">{r.due_at?`Due ${new Date(r.due_at).toLocaleString()}`:'No due date'}</p></div>{r.description&&<p className="mt-2 text-sm text-slate-600">{r.description}</p>}</div>)}{!rows.length&&<p className="p-5 text-slate-500">No assignments yet.</p>}</div>}</section></div></main>
}
